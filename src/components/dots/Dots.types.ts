// eslint-disable-next-line @typescript-eslint/no-namespace
namespace DotsTypes {
  export type Dot = {
    size: `${number}px`
    top: `${number}px`
    left: `${number}px`
  }
}

export default DotsTypes
